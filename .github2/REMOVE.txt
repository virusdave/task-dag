# Retire this repo's vendored task-dag CLI + drift-guard (virusdave/top-level#21).
#
# The canonical task-dag runtime now lives ONLY in virusdave/task-dag. This
# repo consumes it via the .github/workflows/task-dag.yml reusable-workflow
# caller, and agents run it from an `ephemeral-checkout task-dag` checkout
# (canon rules/WORKFLOW.md). These vendored copies + the drift-guard are no
# longer needed and are being removed fleet-wide.
#
# One repo-relative path per line; removals run during promote-github2.sh.
scripts/task-dag
scripts/task-dag.d/cross-repo.sh
scripts/task-dag.d/phase-gates.conf
.github/workflows/task-dag-drift-guard.yml
