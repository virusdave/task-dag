# Retire the now-dead per-repo drift guard. It exists only to fail a peer's
# CI when that peer's *vendored* scripts/task-dag drifts from canonical. No
# repo vendors the CLI any longer (single canonical runtime lives here and is
# run via `ephemeral-checkout task-dag`), so the guard has nothing to guard
# and no caller references it. See virusdave/top-level#21.
#
# Operator: promote with
#   ./scripts/promote-github2.sh virusdave/task-dag
# (one repo-relative path per line; removals run before any moves).

.github/workflows/task-dag-drift-guard.yml
