{
  "name": "Issue to Task Metadata Commit",
  "on": {
    "issues": {
      "types": ["opened", "edited"]
    }
  },
  "permissions": {
    "contents": "write",
    "issues": "write"
  },
  "jobs": {
    "create-task-commit": {
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "uses": "actions/checkout@v4",
          "with": {
            "fetch-depth": 0
          }
        },
        {
          "name": "Create task commit script",
          "run": "cat > /tmp/create-task.sh <<'SCRIPT'\n#!/bin/bash\nset -e\ngit config user.name \"github-actions[bot]\"\ngit config user.email \"github-actions[bot]@users.noreply.github.com\"\nCURRENT_HEAD=$(git rev-parse HEAD)\nEMPTY_TREE=$(git hash-object -t tree /dev/null)\ncat > /tmp/msg.txt <<MSG\nTask: ${ISSUE_TITLE}\n\nIssue: #${ISSUE_NUMBER}\nAuthor: ${ISSUE_AUTHOR}\nURL: ${ISSUE_URL}\nStatus: pending\nType: epic\n\n${ISSUE_BODY}\nMSG\nTASK_COMMIT=$(git commit-tree ${EMPTY_TREE} -p ${CURRENT_HEAD} -F /tmp/msg.txt)\necho \"Created task commit: ${TASK_COMMIT}\"\ngit update-ref refs/heads/tasks/pending/${ISSUE_NUMBER} ${TASK_COMMIT}\ngit push origin refs/heads/tasks/pending/${ISSUE_NUMBER}\ngh issue comment ${ISSUE_NUMBER} --body \"Task metadata commit: ${TASK_COMMIT} | Branch: tasks/pending/${ISSUE_NUMBER}\"\nSCRIPT\nchmod +x /tmp/create-task.sh\n/tmp/create-task.sh"
        }
      ]
    }
  }
}
